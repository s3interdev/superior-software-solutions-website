import Link from 'next/link';

import { format, parseISO } from 'date-fns';

const BlogCategoriesPage = async () => {
	return (
		<>
			<div>Blog Categories Page</div>
		</>
	);
};

export default BlogCategoriesPage;
